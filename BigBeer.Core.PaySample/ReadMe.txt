/***
此类库主要是支付后支付平台将会有一个异步回调，
间隔向你的地址发送一个请求，直到你返回该平台所需要的结果才会停止继续请求
每隔10分钟请求一次，24小时过后将会停止继续请求，
可以在该类库进行数据操作修改，注意保存报错信息，避免无法挽回的损失，
1;请求平台签名前状态为创建，接收到回调首先修改状态为进行中再进行处理
    这样可以有效避险重复请求的问题（个人备注）
***/

A B类型支付可以理解为  余额充值和平台币充值；也就是指订单类型 不同枚举类型调用不同实例